# =============================================================================
# Strix Development Environment Configuration
# =============================================================================
# Copy this file to .env and configure for your local development setup.
# DO NOT commit your actual .env file - it may contain sensitive credentials.
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Configuration
# -----------------------------------------------------------------------------
# Model to use for agent operations (format: provider/model)
# Examples: openai/gpt-4o, anthropic/claude-3-5-sonnet-20241022, openai/gpt-4-turbo
STRIX_LLM=openai/gpt-4o

# LLM request timeout in seconds (default: 600)
LLM_TIMEOUT=600

# -----------------------------------------------------------------------------
# API Keys (required for respective providers)
# -----------------------------------------------------------------------------
# OpenAI API Key (required if using OpenAI models)
OPENAI_API_KEY=

# Anthropic API Key (required if using Anthropic/Claude models)
ANTHROPIC_API_KEY=

# Azure OpenAI (if using Azure-hosted models)
# AZURE_API_KEY=
# AZURE_API_BASE=
# AZURE_API_VERSION=

# -----------------------------------------------------------------------------
# Development Settings
# -----------------------------------------------------------------------------
# Enable debug logging
DEBUG=false

# Python path for module resolution
PYTHONPATH=/app

# Disable Python output buffering for real-time logs
PYTHONUNBUFFERED=1

# -----------------------------------------------------------------------------
# Docker/Sandbox Configuration
# -----------------------------------------------------------------------------
# Enable sandbox mode (runs tools in isolated container)
STRIX_SANDBOX_MODE=false

# Custom sandbox image (optional, uses default if not set)
# STRIX_IMAGE=ghcr.io/usestrix/strix-sandbox:latest

# Docker host (for remote Docker daemon, leave empty for local)
# DOCKER_HOST=tcp://localhost:2375

# -----------------------------------------------------------------------------
# Telemetry & Observability
# -----------------------------------------------------------------------------
# Disable telemetry collection
# STRIX_TELEMETRY_DISABLED=true

# OpenTelemetry endpoint (optional)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# -----------------------------------------------------------------------------
# Proxy Configuration (for testing with intercepting proxy)
# -----------------------------------------------------------------------------
# HTTP proxy for outbound requests
# HTTP_PROXY=http://localhost:8080

# HTTPS proxy for outbound requests
# HTTPS_PROXY=http://localhost:8080

# Bypass proxy for specific hosts
# NO_PROXY=localhost,127.0.0.1

# SSL certificate bundle (for custom CA certs with proxy)
# REQUESTS_CA_BUNDLE=/path/to/ca-bundle.crt
# SSL_CERT_FILE=/path/to/ca-bundle.crt
